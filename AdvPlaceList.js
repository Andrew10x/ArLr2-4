dbConnection = require('./DBSingleton');


class AdvPlaceList {
    conn =  new dbConnection.DBSingleton();
    reservePlaces(arr) {
        for(let i=0; i<arr.length; i++) {
            
        }
    }
}